# 外部大脑维护与优化建议 (Exocortex Maintenance & Optimization Proposal)

**日期**: 2026-02-05
**发起人**: Gemini
**类型**: 维护 (Maintenance) / 优化 (Optimization)

## 1. 现状分析 (Current State Analysis)

在重新索引外部大脑 (`AI_Common/docs`) 后，我发现了以下结构性冗余、过时信息和潜在的逻辑冲突。为了保持“大脑”的清晰与高效，建议进行以下优化。

### 🚨 发现的问题 (Issues Identified)

1.  **`skills/index.md` 存在大量历史噪音**:
    - 文件中包含大量 `✅ 已迁移至标准技能库 (~/.agents/skills/...)` 的备注。
    - 这些是迁移过程中的元数据，现在技能已稳定在 `docs/skills/` 下，这些备注不仅干扰阅读，还指向了不存在的旧路径 (`~/.agents/...`)。

2.  **MCP 工具定义的冲突 (MCP Conflict)**:
    - **Router (`router.md`)** 明确声明：`chrome-devtools-mcp`: **已由 `playwright` 替代** (Deprecated/Legacy)。
    - **Skills Index (`skills/index.md`)** 却将其列为 **核心能力 (Core Capabilities)** 的 `[Low-Level]` 工具。
    - **建议**: 遵循 SSOT (`router.md`)，将 `chrome-devtools` 从核心推荐中移除，或降级为特定调试场景的备选。

3.  **空目录与死链**:
    - `router.md` 或相关结构暗示了 `docs/agents/gemini/skills` 的存在，但实际扫描显示该目录为空。
    - 应当移除此空目录，避免误导检索。

4.  **`Project Initializer` 规则的调用时机**:
    - `router.md` 中定义 Phase 1 (Inception) 加载 `docs/rules/project_initialization_sop.md`。
    - 这是一个很好的设计，建议再次确认该 SOP 是否包含对 `.agent` 目录结构的最新定义（即包含 `plans/` 和 `reviews/` 的新结构）。

## 2. 优化方案 (Optimization Plan)

我建议立即执行以下清理工作：

### ✅ 动作 1: 清理 `docs/skills/index.md`
- **移除** 所有 `✅ 已迁移至...` 的历史备注。
- **移除** 所有指向 `~/.agents/...` 的旧路径引用。
- **修正** `chrome-devtools` 的描述，标记为 (Legacy/Debugging only)，强调 `playwright` 为首选。

### ✅ 动作 2: 维护文件系统
- **删除** 空目录: `docs/agents/gemini/skills`。

### ✅ 动作 3: 路由微调 (`router.md`)
- 确保 `router.md` 中的工具推荐与 `skills/index.md` 保持一致（以 Router 为准）。

## 3. 执行确认 (Confirmation)

老爹，这个优化方案旨在“去噪”并确保指令来源的唯一性 (SSOT)。

**是否授权我执行上述优化动作？**
