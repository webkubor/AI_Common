- **红线规则**: 严禁擅自捏造、猜测或添加任何不存在的外部域名或链接（如 docs.webkubor.xyz）。所有外部链接必须基于用户明确提供的已知事实。
- **红线规则**: 严禁擅自替换或覆盖项目中的 Logo、图片等品牌资产。严禁将示例代码中的测试 URL（如 Paylinker 的 URL）误认为用户的正式资产。所有品牌变更必须基于用户提供的明确文件内容或确切路径。
- **品牌一致性红线**: 整个项目必须保持品牌唯一性。有且只能有一个 Logo 和一个品牌名。在未得到明确指令前，严禁在任何地方（代码、文档、示例）替换、捏造或引入不一致的品牌资产或名称。
## ⚠️ 绝对禁令 (TERMINATION RULES)
- **严禁捏造路由**: 严禁在 `index.html` 或任何地方添加任何非用户提供的外部路由（如 Full Docs）。
- **惩罚逻辑**: 任何此类捏造行为将被视为最高级别安全事故，任务必须立即中断并强制执行全量回滚检查。
