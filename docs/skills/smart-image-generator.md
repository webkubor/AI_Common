# Smart Image Generator (The Unified Visual Engine)

> **Description**: AI Common 的统一视觉引擎。它本身不直接绘图，而是根据 UCD 规范，调用最合适的“底层技能”（如 xhs-vision, nanobanana 或 SVG Executor）执行任务。
> **Status**: **Master Router**

## 1. 🧠 调度逻辑 (Orchestration)

当老爹发起绘图请求时，按以下优先级调度：

| 需求类型 | 核心驱动 (Driver) | 审美/技术标准 (UCD) |
| :--- | :--- | :--- |
| **小红书/美颜/人像** | **[Extension] xhs-vision** | `docs/ucd/persona_system.md` |
| **超高清/场景定制** | **[MCP] nanobanana** | `docs/ucd/persona_system.md` |
| **矢量 Logo/图标** | **[Internal] SVG Coder** | `docs/ucd/logo_design_standard.md` |
| **技术封面/掘金** | **[Internal] Cover Gen** | `docs/ucd/juejin_tech_covers.md` |
| **Slack 动图/GIF** | **[Internal] PIL Engine** | `docs/ucd/slack_gif_standard.md` |

## 2. ⚙️ 执行链路

1.  **意图识别**: 识别是“要漂亮”（xhs）还是“要精准”（nanobanana）。
2.  **规范注入**: 强制读取 `docs/ucd/` 下的对应文件，将里面的参数（如锁相参考图、黄金比例）注入驱动。
3.  **驱动执行**: 调用对应的扩展工具（如 `/xhs` 命令或 `banana_generate`）。
4.  **后处理**: 统一执行桌面保存与 R2 云端上传。

## 3. 📝 开发者备注
*   `xhs-vision` 是本引擎的高级美学插件，专门处理需要中式审美的场景。
*   `nanobanana` 是本引擎的通用物理插件，处理高清、多图参考等底层生成。