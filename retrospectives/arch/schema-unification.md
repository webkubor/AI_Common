### 6. 模型驱动：Schema 统一化重构
- **背景**: 随着双模架构的引入，字段定义分散在 Store、Utils 和本地数据库初始化中，极易导致定义冲突。
- **解决方案**: 创建 `src/types/database.ts` 作为“单一事实来源”。
- **收益**: 消除了类型冗余；实现了 IndexedDB 与 CloudBase 的字段对齐；极大降低了字段扩展的成本。
